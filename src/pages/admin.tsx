import { IonCard, IonCardContent } from "@ionic/react";

export default function Admin() {
  return (
    <IonCard>
      <IonCardContent>
        <h2>Admin</h2>
        <p>Más adelante: CRUD de servicios, agenda, etc.</p>
      </IonCardContent>
    </IonCard>
  );
}
